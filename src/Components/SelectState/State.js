import React from 'react';

function State(props){
    return(
        <div>

            <div>
                {/* <img src={props.url} alt={props.name}></img> */}
            </div>

            <div>{props.name}</div>

        </div>
    )
}
export default State;